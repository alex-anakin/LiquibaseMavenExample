# ѕодключение и использование Liquibase

						Important!
„тобы работать из командной строки, должен быть установлен Maven
и прописаны переменные в Path.  оманда в консоли mvn -version должна выводить
версию Java и версию Maven.


...

10. —оздайте в PostgreSQL вручную базу данных test

11. „тобы запустить плагин, нужно выполнить команду
 
		mvn liquibase:update
		
¬ базе данных test должна по€витьс€ таблица PERSON,
а также 2 служебные таблицы плагина Liquibase

12. „тобы сделать откат (rollback), запустите в командной строке

		mvn liquibase:rollback -Dliquibase.rollbackCount={number}

где {number} - число логов, на которые нужно откатитьс€

13. ѕри работе с готовым проектом команда liquibase:update
выполн€етс€ автоматически при запуске

		mvn clean install
		
